package com.outlawed.repository;

import com.outlawed.model.PreviousCase;
import org.springframework.data.jpa.repository.JpaRepository;

public interface PreviousCaseRepository extends JpaRepository<PreviousCase, String> {
}
